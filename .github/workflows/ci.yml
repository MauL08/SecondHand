name: Go CI Project Process
on:
  push:
    branches: [master]
jobs:
  check-linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install Dependencies
        run: yarn

      - name: Test Linter
        run: yarn lint
